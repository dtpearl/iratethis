<h2><%= @user.name %></h2>

<%= link_to "Share your beautiful food.", new_post_path %>


<% @posts.each do |post| %>
  <%= link_to image_tag(post.post_image, :class => 'feature'), post_path(post.id) %>
  <p><%= post.post_text %></p>
<% end %>
